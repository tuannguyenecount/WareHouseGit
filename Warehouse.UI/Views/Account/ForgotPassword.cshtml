@model ForgotPasswordViewModel
@{
    ViewBag.Title = Warehouse.Language.Account.Index.ForgotPassword;
}


<div class="container">
    <nav data-depth="1" class="breadcrumb">
        <div class="row align-items-center">
            <div class="col">
                <ol itemscope itemtype="http://schema.org/BreadcrumbList">
                    <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
                        <a itemprop="item" href="/">
                            <span itemprop="name">@Warehouse.Language.Header.Header.Home</span>
                        </a>
                    </li>
                    <li itemprop="itemListElement" itemscope="" itemtype="http://schema.org/ListItem">
                        <a itemprop="item" href="@Request.Url">
                            <span itemprop="name">@ViewBag.Title</span>
                        </a>
                    </li>
                </ol>
            </div>
            <div class="col col-auto"> </div>
        </div>
    </nav>
</div>

<div id="inner-wrapper" class="container">
    <div id="content-wrapper">
        <section id="main">
            <header class="page-header">
                <h1 class="h1 page-title">
                    <span>
                        @ViewBag.Title
                    </span>
                </h1>
            </header>
            <section id="content" class="page-content">
                <ul class="ps-alert-error"></ul>

                <header>
                    <p class="send-renew-password-link">@Warehouse.Language.Account.Index.RenewPasswordMessage</p>
                </header>

                <section class="form-fields">
                    @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-inline", role = "form" }))
                    {
                        @Html.AntiForgeryToken()

                        @Html.LabelFor(m => m.Email, new { @class = "required mr-3" })
                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control mb-2 mr-sm-3 mb-sm-0", required = "" })
                        <button class="form-control-submit btn btn-primary" name="submit" type="submit">
                            @Warehouse.Language.Account.Index.SendLink
                        </button>
                        @Html.ValidationSummary("", new { @class = "text-danger" })
                    }
                </section>
                <hr>
                <a href="@Url.Action("Login")" class="account-link">
                    <i class="fa fa-angle-left" aria-hidden="true"></i>
                    <span>@Warehouse.Language.Account.Index.GoBackLoginPage</span>
                </a>
            </section>
            <footer class="page-footer"></footer>
        </section>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
